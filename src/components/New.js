import React, {Component} from 'react';

const New = (noticias) => {

    const {urlToImage, url, title, description, source}=noticias.noticia;

    return(
        <div>
            <p>Desde Noticias</p>
        </div>
    );
}

export default New;